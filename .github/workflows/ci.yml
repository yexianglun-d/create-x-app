name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Node.js ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - run: corepack enable
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: node bin/cli.js template lint
      - run: node bin/cli.js template test --template node-ts
      - run: npm run test:integration
      - run: npm run test:smoke
      - run: npm pack --dry-run

  windows-smoke:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run test:smoke
      - run: node bin/cli.js template test --template node-ts
