# CLAUDE.md

Project: <%= projectName %>

## Working Rules

- Use the existing stack and package manager: `<%= packageManager %>`.
- Keep changes scoped to the requested behavior.
- Run the closest validation command before reporting completion.
- Do not hide failures with broad fallbacks or silent catch blocks.
- Avoid committing `.env`, local secrets, build output, or dependency directories.

## Project Context

- Template: `<%= template %>`
- Generated by: `create-x-app`
- Shared conventions: see `AGENTS.md` and `coding-rules.md`.
