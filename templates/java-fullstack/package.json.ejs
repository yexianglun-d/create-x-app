{
  "name": "<%= projectName %>",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {<% if (hasHusky) { %>
    "husky": "^9.0.11",
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2"<% } %>
  }
}
